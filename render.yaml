services:
  - type: web
    name: biodiversity-ontology-generator
    runtime: python
    buildCommand: python -m pip install --upgrade pip && pip install -r requirements.txt && pip install gunicorn
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9